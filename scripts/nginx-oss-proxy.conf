# Add to /etc/nginx/conf.d/ibiren.conf — proxy /ips/ to OSS public bucket
# so users can access ibi.idata.mobi/ips/<code>/thumb_600.jpg directly

# In server { listen 8080 ... } block, BEFORE location /:
#     # OSS public bucket 反代 (用户访问 ibi.idata.mobi/ips/... 直通 OSS, 不走 SPA fallback)
#     location /ips/ {
#         proxy_pass https://ibi-public.oss-cn-shanghai.aliyuncs.com;
#         proxy_set_header Host ibi-public.oss-cn-shanghai.aliyuncs.com;
#         proxy_ssl_server_name on;
#         proxy_redirect off;
#         expires 30d;
#         add_header Cache-Control "public, max-age=2592000" always;
#     }

# Also handle /lora/, /3d/ etc if/when needed. For now just /ips/.
